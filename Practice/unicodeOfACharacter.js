// Unicode of a character

function character() {
	Number ch = code;
	return ch;
}

function unicode() {
	ch = code;
	return ch;
}

let first = 'A';
let second = 65;
        console.log("The Unicode for the character " + first + " is: '" + character(first) + "'");
	console.log("The character for the unicode " + second + " is: '" + unicode(second) + "'");

