// Looping a triangle

let asterisks = "#";

for(let count = 1; count <= 7; count++){
	let temp = " ";
	for(let counter = 0; counter < count; counter++){
	temp += asterisks;
}
	console.log(temp);
};